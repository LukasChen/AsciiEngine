#pragma once
#include <vector>
#include "math3d.h"
#include "registry.h"
#include "system.h"

class Engine {
public:
    Registry registry;
private:
    // std::vector<std::unique_ptr<System>> systems;
};
